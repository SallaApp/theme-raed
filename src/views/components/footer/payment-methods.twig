{#
| Variable        | Type  | Description                                                     |
|-----------------|-------|-----------------------------------------------------------------|
| payment_methods | array | List of active payments ex: [mada, credit_card, bank (Bank transfer (Transfer receipt)), cod (Cash On Delivery), ...] |
#}
<ul class="payment-methods-list flex justify-center items-stretch flex-wrap mb-5 lg:mb-0 -mx-1">

    {#    Made in KSA certificate#}
    {% if store.settings.made_in_ksa %}
        <li class="payment-item w-14 bg-white rounded flex-center p-1 m-1">
            <img class="object-contain max-h-[24px]" decoding="async" loading="lazy" src="{{ 'images/made-in-ksa.svg' | cdn }}" alt="made in KSA certified">
        </li>
    {% endif %}

    {% for method in payment_methods %}
        <li class="payment-item w-14 bg-white rounded flex-center p-1 m-1">
            <img class="object-contain max-h-[24px]" src="{{ ('images/payment/'~ method ~ '_mini.png')|cdn }}" loading="lazy" width="36" height="24" alt="{{ method }}">
        </li>
    {% endfor %}
    {# Business certificate - SBC #}
    {% if store.settings.certificate %}
        <li class="payment-item w-14 bg-white rounded flex-center p-1 m-1">
            <a class="load-img-onclick" data-modal-id="modal-certificate" href="#/" alt="{{ store.settings.certificate.id }}">
                <img width="100%" height="100%" src="{{ 'images/s-empty.png' | asset }}" data-src="{{ 'images/sbc.png' | cdn }}" class="lazy rounded-sm hover:opacity-80 transition-opacity max-h-6" alt="{{ store.settings.certificate.id }}">
            </a>
        </li>

        <salla-modal sub-title-first sub-title="{{ store.settings.certificate.id }}" modal-title="{{ store.settings.certificate.id }}" id="modal-certificate">
            <div class="flex justify-center max-w-full">
                <img class="w-full" loading="lazy"
                     src="{{ 'images/s-empty.png' | asset }}"
                     data-src="{{ store.settings.certificate.image }}"
                     alt="{{ store.settings.certificate.id }}"/>
            </div>
        </salla-modal>
    {% endif %}
</ul>
