{#
| Variable                       | Type    | Description                                                               |
|--------------------------------|---------|---------------------------------------------------------------------------|
| component                      | object  | Contains merchant settings for fields from theme.json `component` section |
| component.brands               | Brand[] | list of selected brands @see views/pages/brands/single.twig               |
| position                       | int     | Sorting number start from zero                                            |
#}
<section class="s-block s-block--logos-slider s-block--full-bg bg-gray-1002 pt-8 sm:pt-12 pb-8 sm:pb-20">
    <div class="container">
        <div class="center-between mb-6 sm:mb-8">
            <h1 class="s-block__title">{{trans('blocks.home.browse_brands')}}</h1>
            <a href="{{link('brands')}}" class="s-block__display-all"> {{trans('blocks.home.display_all')}} </a>
        </div>

        <div class="grid grid-cols-2 {{component.brands|length > 4 ? 'md:grid-cols-4' : 'md:grid-cols-' ~ component.brands|length }} grid-flow-row lg:grid-flow-col gap-4 lg:gap-8">
            {% for brand in component.brands %}
                {% if loop.first or loop.index0%3==0 %}
                    <a href="{{ brand.url }}"
                       class="brand-item {{component.brands|length > 4  ? 'sm:row-span-2 sm:h-auto': 'md:h-banner' }}">
                        <img class="max-h-full" src="{{ brand.logo }}" alt=""/>
                    </a>
                {% else %}
                    <a href="{{ brand.url }}"
                       class="brand-item">
                        <img class="max-h-full" src="{{ brand.logo }}" alt=""/>
                    </a>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</section>
