{#
| Variable        | Type       | Description                    |
|-----------------|------------|--------------------------------|
| products        | Products[] |                                |
| position        | Int        | Sorting number start from zero |
#}
<section class="s-block s-block--latest-products container mt-6 sm:mt-8">
    <div class="mb-6 sm:mb-8">
        <h2 class="s-block__title">{{ trans('blocks.home.latest_products') }}</h2>
    </div>
    {% if products|length %}
        <salla-infinite-scroll class="gird-view grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2.5 sm:gap-4 md:gap-6 mb-6"
                               next-page="{{ products.next_page }}"  autoload>
            {% for product in products %}
                {% include 'pages.partials.product.card' %}
            {% endfor %}
        </salla-infinite-scroll>
    {% endif %}
</section>