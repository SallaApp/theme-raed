{#
| Shared salla-menu component to prevent duplicate API calls
| Usage: {% include 'components/shared/salla-menu.twig' with { 'classes': 'additional-classes' } %}
#}
<salla-menu source="footer" {% if topnav %}topnav{% endif %} class="{{ classes|default('') }}" id="shared-salla-menu"></salla-menu>