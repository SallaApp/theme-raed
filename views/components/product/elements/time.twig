<input placeholder="{{ option.placeholder }}"
       id="receiveTime"
       class="form-input time-element"
       name="options[{{ option.id }}]"
        {{ attirubtes|raw }}
       readonly="readonly"
       value="{{ value }}"
       type="text"/>
