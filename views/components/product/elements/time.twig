<input placeholder="{{ option.placeholder }}"
       class="form-control time-element"
       name="options[{{ option.id }}]"
        {{ attirubtes|raw }}
       readonly="readonly"
       value="{{ value }}"
       type="text"/>
