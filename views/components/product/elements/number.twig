<input placeholder="{{ option.placeholder }}"
       name="options[{{ option.id }}]"
       class="form-input"
       value="{{ value }}"
       type="text"
       data-digits
        {{ attirubtes|raw }}
/>