<input placeholder="{{ option.placeholder }}"
       name="options[{{ option.id }}]"
       class="form-control"
       value="{{ value }}"
       type="text"
       data-digits
        {{ attirubtes|raw }}
/>