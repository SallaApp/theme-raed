<div class="product-brand mb-5 w-20">
    <img class="max-h-full object-contain lazy-load" src="{{ asset('images/s-empty.png') }}" data-src="{{ product.brand.logo }}" title="{{ product.brand.name }}" alt="{{ product.brand.name }}">
</div>