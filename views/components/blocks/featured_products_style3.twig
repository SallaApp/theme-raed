<section class="home-block  home-block--featuresProducts mb-20 container {{ is_repeated?'repeated-block pt-0':'' }}">
    <div class="grid lg:grid-cols-2 gap-5">
        {% for section in items %}
            <div>
                <div class="flex justify-between mb-8">
                    <h2 class="font-bold text-lg text-text-dark">{{ section.title }}</h2>
                </div>
                <div class="grid lg:grid-cols-2 gap-5">
                    {% if section.featured_product %}
                        {% include 'pages.partials.product.card-full-image' with {product:section.featured_product} %}
                        <div class="grid gap-5">
                            {% for product in section.products|slice(0,3) %}
                                {% include 'pages.partials.product.card-mini' %}
                            {% endfor %}
                        </div>
                    {% else %}
                        {% for product in section.products %}
                            {% include 'pages.partials.product.card-mini' %}
                        {% endfor %}
                    {% endif %}
                </div>
            </div>
        {% endfor %}
    </div>
</section>